# Copy this file to backend/.env.inventory (or merge these keys into backend/.env).
# Keep SCM credentials on the Go backend only. Never expose them as NEXT_PUBLIC_*.

SCM_API_URL=https://scm.fiberathome.net/scm/api/item-search-by-mr.php
SCM_API_TOKEN=PASTE_CURRENT_SCM_TOKEN_HERE
SCM_TIMEOUT_SECONDS=15
